[tool.pytest.ini_options]
# Ensures `from app import create_app` resolves in tests regardless of
# where pytest is invoked from, without needing an __init__.py in tests/.
pythonpath = ["."]
asyncio_mode = "auto"
filterwarnings = [
    "ignore:Using .httpx. with .starlette.testclient. is deprecated:"
]